> [!IMPORTANT]
> Avoid rewriting published git history on `main` — force pushing, or
> rebasing/amending/squashing commits that are already pushed — since this
> branch is synced with an external editor and rewriting history there can
> lose project history.
>
> Keep the branch in a working state, since every push syncs through.
